package main

import "fmt"

func main() {
	//变量定义 var
	//常量定义 const
	var name string
	var gender = "man"
	age := 23
	name = "duke"

	fmt.Println(name)
	fmt.Printf("name is %s,and gender is %s and age is %d \n", name, gender, age)
}
